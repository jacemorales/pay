# This file contains all the environment variables needed to run the application.
# For local development, you can copy this file to a new file named .env
# and fill in the values. For production on Netlify, you must set these
# variables in the Netlify UI.

# Paystack Configuration
PAYSTACK_SECRET=sk_test_...
APP_URL=http://localhost:8888

# Database Configuration (for a cloud-hosted MySQL database)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306

# Email Configuration (for SMTP, e.g., Mailtrap or Google)
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="Payment Platform"
