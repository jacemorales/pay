{
  "name": "payment-platform-backend-services",
  "version": "1.0.0",
  "private": true,
  "description": "Backend services for the payment platform.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "mysql2": "^2.3.3",
    "nodemailer": "^6.7.2"
  }
}
